version: "3"
services:
  snapshotter:
    image: "segment/snapshotter:v0.0.1"
    healthcheck:
      test: "curl --silent --show-error --fail localhost:8765/messages || exit 1"
    ports:
      - "8765:8765"
