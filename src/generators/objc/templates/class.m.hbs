/**
 * {{autogeneratedFileWarning}}
 */

#import "{{name}}.h"

@implementation {{name}}

+(nonnull instancetype) {{functionSignature "init" properties false}} {
  {{name}} *object = [[{{name}} alloc] init];
  {{#each properties}}
  object.{{name}} = {{name}};
  {{/each}}
  return object;
}

-(nonnull SERIALIZABLE_DICT) toDictionary {
  {{indent "  " (propertiesDictionary properties "self.")}}

  return properties;
}

@end
