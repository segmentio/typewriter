/**
* {{autogeneratedFileWarning}}
*/
package com.segment.generated;

import java.util.*;
import com.segment.analytics.Analytics;
import com.segment.analytics.Options;
import com.segment.analytics.Properties;
import android.content.Context;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;


public class SEGTypewriterAnalytics {
  private Analytics analytics;

  public SEGTypewriterAnalytics(final @NonNull Analytics analytics) {
    this.analytics = analytics;
  }

{{#each tracks}}
{{!-- TODO: add callsite with no props --}}
  {{#if functionDescription}}
  // {{functionDescription}}
  {{/if}}
  {{functionSignature this false}} {{functionExecution this false}};

  {{#if functionDescription}}
  // {{functionDescription}}
  {{!-- TODO: description of what options are --}}
  {{/if}}
  {{functionSignature this true}} {{functionExecution this true}};
  
{{/each}}
}