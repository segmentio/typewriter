/**
* {{autogeneratedFileWarning}}
*/
package com.segment.generated;

import java.util.*;
import com.segment.analytics.Properties;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

public final class {{name}} extends Serializable {
  private Properties properties;

  {{propertiesGetterSetter name}}

  /**
  * Builder for {@link {{name ~}} }
  */
  public static class Builder {
    private Properties properties;

    /**
    * Builder for {@link {{name ~}} }
    */
    public Builder() {
      properties = new Properties();
    }

  {{#each properties}}

    /**
     {{#if description}}
     * {{description}}
     {{/if}}
     {{#if isRequired}}
     * This property is required to generate a valid {{../name}} object
     {{/if}}
     */
    {{builderFunctionSignature name modifiers type}} {
    {{builderFunctionBody name rawName type}}
    } 
    
  {{/each}}

    /**
    * Build an instance of {@link {{name ~}} }
    */
    public {{name}} build() {
      return new {{name}}(properties);
    }
  }
}