/**
* {{autogeneratedFileWarning}}
*/
package com.segment.generated;

import java.util.*;
import com.segment.analytics.Properties;
import androidx.annotation.NonNull;

public final class {{name}} {
private Properties properties;

private {{name}}(Properties properties) {
this.properties = properties;
}

protected Properties toProperties() {
return properties;
}

/**
* Builder for {@link {{name}}}
*/
public static class Builder {
private Properties properties;

/**
* Builder for {@link {{name}}}
*/
public Builder() {
properties = new Properties();
}

{{#each properties}}
{{#if description}}
/**
*{{description}}
*/
{{/if}}
public Builder {{name}}({{modifiers}} {{type}} props) {
properties.putValue({{name}}, props)
}
{{/each}}

/**
* Build an instance of {@link {{name}}}
*/
public {{name}} build() {
return new {{name}}(properties);
}
}
}