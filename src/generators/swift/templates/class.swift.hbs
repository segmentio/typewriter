/**
 * {{autogeneratedFileWarning}}
 */

import Foundation

class {{name}}: TypewriterSerializable {
{{#each properties}}
{{#if description}}
/// {{description}}
{{/if}}
    var {{name}}: {{type}}{{#isVariableNullable}}?{{/isVariableNullable}}
{{/each}}

    {{functionSignature "init" properties false}} {
        {{#each properties}}
        self.{{name}} = {{name}}
        {{/each}}
    }

    func serializableDictionary() -> [String: Any] {
        {{indent "        " (propertiesDictionary properties "self.")}}

        return properties;
    }
}