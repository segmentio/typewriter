/**
 * {{autogeneratedFileWarning}}
 */

#import "SEGTypewriterAnalytics.h"
#import "SEGTypewriterUtils.h"

@implementation SEGTypewriterAnalytics

{{#each tracks}}
{{#if description}}
/// {{description}}
{{/if}}
+ (void){{functionSignature functionName properties false}}
{
    {{functionCall "SEGTypewriterAnalytics" functionName properties "options" "@{}"}}
}

{{#if description}}
/// {{description}}
{{/if}}
+ (void){{functionSignature functionName properties true}}
{
    {{indent "    " (propertiesDictionary properties)}}

    {{!-- TODO: run-time JSON Schema validation --}}
    [[SEGAnalytics sharedAnalytics] track:@"{{rawEventName}}" properties:properties options:[SEGTypewriterUtils withTypewriterContextFields:options]];
}

{{/each}}
@end
